# Ruff configuration
line-length = 100
target-version = "py312"

[lint]
select = ["E", "F", "I", "N", "W", "UP"]
ignore = ["W293"]  # Allow whitespace in blank lines (docstring formatting)

[lint.per-file-ignores]
"__init__.py" = ["F401"]
